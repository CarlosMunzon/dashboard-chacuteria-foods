# Dashboard Streamlit

Ejecutar con:

```bash
pip install -r requirements.txt
streamlit run app.py
```

La carpeta `data/` contiene los insumos ya limpios. Reemplazando esos archivos se puede refrescar el dashboard.
